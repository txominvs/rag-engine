## Setup
1. Launch containers: `docker compose up -d`
2. Pull the Ollama LLM models from the internet: `./scripts/pull_models.sh`

## TODOs
- FastAPI for ingest & query (barebones / LangChain endpoints)
- Make shared folder for storing actual docs
- LangFuse + Guardrails for trazability + security
- FastMCP for tools
- Milvus as an alternative vectorDB
- nginx frontend
- Reranker & Hybrid Search (BM25)